1. noReferences.fsx		// Demonstration of a simple counter without references
2. noReferencesFail2.fsx	// Attempt of incr on a mutable fails, since return is copy by value, hence it's not inside incr
3. noReferencesFail.fsx		// Attempt of makeCounter fails, since return is copy by value, hence it's not mutable outside makeCounter
4. references.fsx		// Demonstration of mutable records
5. references2.fsx		// Demonstration of reference variables
6. references3.fsx		// Variant of demonstration of reference variables
7. sideEffects.fsx		// Demonstration of side effect of mutable records
8. listIter.fsx			// Examples of list iterations
9. treeTraversal.fsx		// Examples of binary tree traversals
