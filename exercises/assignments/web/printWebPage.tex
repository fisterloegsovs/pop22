Make a program with the function,
\begin{quote}
  \mbox{\lstinline!printWebPage : url:string -> string option!}
\end{quote}
which reads the content of the internetpage \lstinline!url! and
returns its content as a string option.
