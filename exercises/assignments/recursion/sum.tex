Write a function \lstinline{sumRec : n:uint -> uint} that calculates the sum $\sum_{i=0}^ni$ using recursion.
%Extend your table from Assignment~3g0 with a column for \lstinline{sumRec} and compare this function with \lstinline{sum} and \lstinline{simpleSum}.
