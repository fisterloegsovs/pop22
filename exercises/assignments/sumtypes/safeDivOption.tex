Implement a function \lstinline{safeDivOption : int -> int -> int option} that takes two integers $a$ and $b$ as arguments and returns
\lstinline{None} if $b$ is 0 and the value
\lstinline[mathescape]{Some($a/b$)}, otherwise.
