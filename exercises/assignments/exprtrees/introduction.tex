The following exercises are about expanding and using the following
recursive sumtype, which can be used for modelling expression terms:
\begin{lstlisting}
type expr = Const of int | Add of expr * expr | Mul of expr * expr
\end{lstlisting}
