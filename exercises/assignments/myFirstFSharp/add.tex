Start en interaktiv F\# session og indtast følgende (efterfulgt af ny linie):
  \begin{codeNOutput}{: My first F\#.}
\begin{lstlisting}
3.14+2.78;;
\end{lstlisting}
  \end{codeNOutput}
  Beskriv (for dig selv), hvad F\# gjorde, og hvis der opstod en fejl, find fejlen og gentag.
